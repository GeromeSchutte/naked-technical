type Todo @model {
  id: ID!
  name: String!
  description: String
}

enum ItemType {
  Home
  Car 
  Phone
  Camera
}

type Item @model {
  itemId: ID!
  title: String!
  subtitle: String!
  itemType: ItemType!
  premium: Float!
}

type Basket @model {
  basketId: ID!
  startDate: AWSDate!
  items: [Item]!
  totalPremium: Float!
}